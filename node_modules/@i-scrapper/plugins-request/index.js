const { fetch } = require("./lib");

const Request = function Request(options) {
    this.arguments = arguments;
    this.options = options;
    return this.send();
};

Request.prototype.send = function send() {
    return fetch(this.options);
};

module.exports = (options = {}) => new Request(options);